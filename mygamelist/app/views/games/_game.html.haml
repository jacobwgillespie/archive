%li.span2
  = link_to game do
    = image_tag game.poster_url(:list), :class => 'poster'
    .hideme
      %h4= game.name
      %h5= game.release_date.strftime('%b %e, %Y') if game.release_date
  .who
    = image_tag current_user.avatar, :width => 20
    %strong= current_user.name