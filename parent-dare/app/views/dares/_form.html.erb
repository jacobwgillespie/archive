<%= form_for(@dare) do |f| %>
  <% if @dare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dare.errors.count, "error") %> prohibited this dare from being saved:</h2>

      <ul>
      <% @dare.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :devotion %><br />
    <%= f.text_area :devotion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
